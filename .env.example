# Copy to .env and fill in. .env is gitignored.

# LLM providers (need the one matching settings.yaml -> llm.provider)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OLLAMA_HOST=http://localhost:11434

# Tweet inspiration source.
# Only needed if tweet_source.adapter = official_x. See README "Compliance notes".
X_BEARER_TOKEN=
# Only needed if tweet_source.adapter = unofficial_x. Violates X ToS — use at your own risk.
TWITTERAPI_IO_KEY=
